#+TITLE: Simple Data Table Functions for Emacs TODO List

* Inbox
** TODO tblfn-add-footer-sum|vcalcに:calcoptを指定可能にする
単純に ~:calcopt~ キーワード引数を追加するだけだと、個別の列に対して別々の書式を指定できない。
例えば2列目は12.3万円のように小数点以下1桁で3列目は23.45%のように2桁であるとか。

- (tblfn-add-footer-sum table ("Price" :calcopt 1) ("Percentage" :calcopt 2))
- (tblfn-add-footer-vcalc table nil ("Price" "vsum" :calcopt 1) ("Percentage" "vsum" :calcopt 2))
みたいな感じ？

** TODO tblfn-aggregateで複数の列を集計できるようにする
現在は個別に集計して結合しなければならない。
tblfn-aggregate-columnsのような関数を追加することもできるが、VALUE-COLSPEC-OR-FUNCTIONに複数指定できるようにした方が使いやすい気もする。

VALUE-COLSPEC-OR-FUNCTIONは現状では:
- COLSPEC
  - integer
  - string
- FUNCTION
  - S-expression (最初の要素が関数)
  - Function

なのでこれを複数にすることは一応はできる:
: ( INTEGER|STRING +|SEXP|FUNCTION+ |(INTEGER|STRING|SEXP|FUNCTION [VFUN [COLNAME]] [:calcopt CALCOPT] ) ... )

リストの先頭がintegerp、stringp、conspであるなら複数列指定、functionpなら従来のS式と判定する。

その場合、VFUN、NEW-VALUE-COLNAME、CALCOPT引数が無駄になる。省略時のデフォルト値として活用しても良い。

** TODO tblfn-sortは空のフィールドがあるとソートできない
** TODO tblfn-uniqueに比較関数を設定できるようにする
キーが同じものは最初のものだけを残すようにしたい。
冗長なテーブルだと同じキーなら他の列も同じである場合があり、その列の値とキーの対応表を作りたい場合がある。例えば価格表のようなもの。同じ商品は現在の単価も同じなはず。そこから商品と現在の単価の表を作るとか。

** TODO calc-evalで実行する式に「引数」を渡す方法を調べる
** TODO 日付に対応する
** TODO tblfn-add-sum-column
** TODO tblfn-add-vcalc-column
** TODO tblfn-add-calc-column
** TODO tblfn-add-footer-vcalcでは列ごとに関数を指定できない
** TODO insert-columns-at関数を実装する
現状では[[elisp-function:tblfn-append-columns]]はあるけど途中に挿入できない。

** TODO 多くのbody系関数でtable引数にnilを指定した結果の列名を何とかする
現在はbodyを対象とした関数でtable引数にnilを指定した場合ヘッダーがnilなる。
#+begin_src elisp :results pp
(tblfn-add-body-row nil '(0 1 2))
#+end_src

#+RESULTS:
: (nil hline (0 1 2))

これはある意味当然の仕様にも思えるが、現実問題これでは列数が0になってしまうので、次のテーブル操作がまともにできない。[[elisp-function:tblfn-add-header-row]]はダミーの列名を生成すべきではないだろうか？

** TODO 全体を文字列化する関数を追加する
** TODO org-modeから使うときに数値を文字列のままにできないか
* Finished
** CANCELLED footerをできるだけキープすべき？
CLOSED: [2026-01-03 Sat 23:40]
無理。どうしても効率が悪くなるので、必要なら最後にくっつけるべき。
(nconc (copy-sequence result-table) (tblfn-footer-hline-and-after original-table))
** DONE README.orgを書く
CLOSED: [2026-01-05 Mon 23:58]
** DONE 計算結果を自動的に数値にすべきか
CLOSED: [2026-01-06 Tue 12:16]

tblfn-calc-result-number-type変数を追加した。
** DONE キーワード引数を処理するマクロを作る
CLOSED: [2026-01-06 Tue 20:58]
cl-defunは、option,restとkeyを同時に使うのが困難。

[[https://stackoverflow.com/questions/56712241/use-of-rest-and-key-at-the-same-time-in-common-lisp][use of &rest and &key at the same time in Common Lisp - Stack Overflow]]

#+begin_src elisp
(defun test (&rest args)
  (let* ((keys (member-if #'keywordp args))
         (nonkeys (ldiff args keys)))
    (destructuring-bind (&key name) keys
      (list nonkeys name))))
#+end_src

tblfn--let-argsを作った。
** DONE tblfn-joinのキーワード引数化
CLOSED: [2026-01-06 Tue 20:58]
tblfn--let-argsを使った。
** DONE 全フィールドに関数を適用する関数を追加する
CLOSED: [2026-01-10 Sat 17:34]
tblfn-map-field
** DONE 全フィールドを可能な限り数値化する関数を追加する
CLOSED: [2026-01-10 Sat 17:34]
tblfn-numberize
** DONE tblfn-data-row-pをリストに限定するかどうか(データ行の定義)
CLOSED: [2026-01-10 Sat 17:58]
本来であればリストに限定すべき。
しかしTEST:の多くの部分で非リストの行を使っているのでそれを全て直さなければならない。
** DONE ヘッダーに列名以外(複数行)がある時を考慮する
CLOSED: [2026-01-10 Sat 20:22]
今は多くの関数でtblfn-add-header-rowの第二引数に(tblfn-column-names table)を引き渡している。しかし本来は元のテーブルのヘッダー行を丸丸復元すべきではないか。つまりhlineが使われている場合でかつその前に複数行ある場合はそれを全て復元すべきでは無いか。

- tblfn-header関数を追加し、hlineを含むヘッダー部分を返す。必ず新しいリストで。
- tblfn-prepend-header関数を追加し、(nconc (tblfn-header table) body)をする。
** DONE tblfn-insert-columnとtblfn-add-columnの初期値に式を書けるようにする
CLOSED: [2026-03-31 Tue 20:40]
initial-valueをcolumn-initializer-specに変更し、文字列と整数の他、S式や関数を書けるようにする。関数は行を引数に取ってフィールド値を一つ返す。
** DONE :padding-value引数は:paddingに変更すべき
CLOSED: [2026-03-31 Tue 20:55]
長過ぎなので。
互換性無くなるけどいいや。どうせ使ってる人なんていないでしょう。
頑張れば互換性を保てるだろうけどコードが汚くなるし止めておく。
** DONE tblfn-joinで右側のマッチしなかったものを含める
CLOSED: [2026-03-31 Tue 23:13]
:include-mismatchにrightやboth(またはfull)を指定したら、右側のテーブルの左とマッチしなかったものを結果に含めるようにする。
過去の保有リストと今の保有リストを並べて比較したいような場合に、左右両方のマッチしないもの(つまりすでに売ったものや新規で買ったもの)も表に含めたいようなケース。
** DONE tblfn--let-argsの不具合を修正する
CLOSED: [2026-05-24 Sun 10:59]
(tblfn--let-args '(&key calcopt) rest-args ...)のように書くとcalcopt変数が複数作成されることがある。

原因はtblfn--let-args-make-bindings関数内のnconc部分にある。
引数の数が足りない場合のコードを生成しているが、その部分が
: (nconc
:   ...
:   '((_ (when positional-args
:     (signal 'wrong-number-of-arguments (list nil nil)))))
となっているため、'( ... ) で書いたリストを破壊してしまう。
** DONE calc-evalに引き渡すオプションを指定出来るようにする
CLOSED: [2026-05-24 Sun 12:13]
~:calcopt~ キーワード引数を追加する。

- tblfn-column-sum
- tblfn-column-vcalc
- tblfn-aggregate
- tblfn-add-percentage-column

次の関数や変数を整備:
- tblfn-calc-vector-fun
- tblfn-calcopt-parse
- tblfn-calcopt-default変数
- tblfn-calc-eval

次の関数は保留(See: [[*tblfn-add-footer-sum|vcalcに:calcoptを指定可能にする][tblfn-add-footer-sum|vcalcに:calcoptを指定可能にする]]):
- tblfn-add-footer-sum
- tblfn-add-footer-vcalc
